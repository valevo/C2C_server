# Deploy to /etc/ssh/sshd_config.d/10-c2c.conf
# Only applied by bootstrap.sh once an authorized key exists for `admin` —
# otherwise you lock yourself out.
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin no
AllowUsers admin c2c
ClientAliveInterval 60
ClientAliveCountMax 3
